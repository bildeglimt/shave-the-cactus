name: 'Shave The Yak'

on:
  issues:
    types: [opened]

jobs:
  shave:
    runs-on: ubuntu-latest
    steps:
      - name: Get Username
        run: echo "${{github.event.sender.login}}"
      - name: Get ID
        run: echo "${{github.event.sender.id}}"
